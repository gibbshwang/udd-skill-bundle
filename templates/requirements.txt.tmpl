playwright>=1.48
pyyaml>=6.0
pandas>=2.2
openpyxl>=3.1
keyring>=25.0
requests>=2.32
# Default AI provider is Codex CLI, invoked as an already-installed subprocess.
# Optional direct external API providers:
# anthropic>=0.40
# google-generativeai>=0.8
# openai>=1.50
